// import { Outlet } from "react-router-dom";

import { Outlet } from "react-router-dom";

function App() {
  return (
    <>
     
      <Outlet /> {/* 👈 Public pages (Landing, Login, Teacher Login, etc.) */}
    </>
  );
}

export default App;
